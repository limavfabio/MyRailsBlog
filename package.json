{
  "devDependencies": {
    "stylelint": "15.x",
    "stylelint-scss": "3.x",
    "stylelint-config-standard": "21.x",
    "stylelint-csstree-validator": "1.x"
  },
  "scripts": {
    "lint:css": "stylelint \"**/*.{css,scss}\" --config .stylelintrc.json --fix",
    "lint:all": "rubocop -A && pnpm run lint:css"
  }
}
